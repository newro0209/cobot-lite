# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트 개요

cobot-lite — 1kg 페이로드급 절충형(palletizing-type) 협동 로봇 팔. 대규모 디스코프(2026-06)로 2링크 체인까지 축소됐다가, **v0.17(2026-06-13)에서 팔레타이저식 하부 구동 3축(J1 요 + J2/J3 피치)으로 재확장** — J1~J3 모터 전부 베이스측 고정 구조물 장착(이동부 모터 0), J3는 크랭크→구동 링크→혼 평행사변형 원격 구동, 상완/하완은 꺾임 빔(기구학 오프셋). 산출물: 기구 설계(OpenSCAD)와 BOM. 순수 모델링 프로젝트다. (손목/공구, 그리퍼는 여전히 범위 외.)

**범위 외 (구현 금지)**: 역기구학(IK), 경로 계획, G-code 생성, 그리퍼 설계, 안전 기능(E-stop, 충돌 감지 등), 전장 설계(컨트롤러·드라이버·전원), 제어 펌웨어 (GEN-001, §7).

## 요구사항 문서가 최상위 권위

[REQUIREMENTS.md](REQUIREMENTS.md)가 모든 설계 결정의 기준. RFC 2119 마커(MUST/SHOULD/MAY) 사용. 작업 전 관련 요구사항 ID를 확인하고, 충돌 시 우선순위 스택 적용 (§0.2):

1. 부품 제약 (CON-*) — 최우선
2. 기능 (FUN-*/MEC-*)
3. 성능 (PER-*)
4. 제작/비용 (MFG-*, CST-*)
5. 프로젝트 관리 (PRJ-*, DOC-*)

SHOULD 예외를 둘 때는 사유를 `docs/`에 기록 (DOC-001). 값이 [TBD-n]으로 표시된 항목은 §11 TBD 목록 확인 후 임의 확정하지 말 것.

## 핵심 부품 제약 (모든 설계 판단에 선행)

- **피벗 샤프트**: 스트리퍼 볼트(숄더 볼트) + 와셔만. 숄더 5mm(625ZZ) / 8mm(608ZZ) 병용 가능. 환봉·전산볼트 금지 (CON-001)
- **베어링 4종만**: 608ZZ, 625ZZ, 6805ZZ, 6810ZZ. (CON-002/003)
- **모터**: 산요덴키 SF24 시리즈 3종만 (SF2424/SF2423/SF2422). 모터 + 기성품 유성 기어박스(planetary gearbox) 직결, 감속비 5:1/10:1/20:1만. 벨트/풀리 금지 (CON-004/005). 축↔구동부 결합 = **기성 플랜지 커플러(flange coupler, set-screw)** — 기어박스 ⌀8 출력축을 무두 볼트로 grip, 플랜지를 구동 부품에 셀프태핑 M4 ×4 볼트온 (v0.18, 분할 클램프 폐지)
- **중력 보상 기구 금지** (스프링, 카운터웨이트 등). 단, 기존 부품(모터·전장) 배치를 균형에 유리하게 조정하는 것은 허용 (CON-009) — v0.17은 모터 3기 전부를 베이스측에 두어 이동 질량을 최소화 (평행사변형 = 구동 전달 기구이지 중력 보상 아님)
- 구조 부품은 PETG-CF 출력 (CON-006). 구매 부품은 AliExpress/국내 유통 가능품만 (CON-008)

## 아키텍처

### 기구 (OpenSCAD)

- `config/parameters.scad`가 단일 치수 소스. 모든 출력 부품 치수는 전역 파라미터로 제어 — **매직 넘버 하드코딩 금지** (MEC-008). 베어링 압입 공차(-0.1~-0.2mm)도 단일 변수로 노출 (MFG-002)
- `parts/` — 1 파일 1 부품군 (다피스 부품은 파일 내 `piece` 셀렉터). `lib/utils.scad` — 공통 모듈(베어링 시트, 볼트 홀, 꺾임 빔 프로파일 `bent_beam2d`, 아크 슬롯 등). `assembly/assembly.scad` — 전체 조립·간섭 확인용
- 구조 핵심 (v0.17): **J1** = 베이스판 하면에 A1(SF2422+G20) 수직 직결(매달림 — 지면판과 사이 L115 스탠드오프 ×4가 수납), 출력축 ⌀8 → 플랜지 커플러 → 터릿 보스(⌀50). **6810ZZ = 회전 내륜 방식**: 외륜이 j1_ring(고정)에 압입, 내륜이 터릿 하면 ⌀50 보스(회전). 모멘트 경로: 암→터릿→내륜→볼→외륜→링→베이스 (구 ⌀41 허브가 6810 보어 공극에 볼트되던 결함 해소). 터릿↔링 0.3 축 간극(6810 축하중 부담). **J2** = -Y 타워 외면에 A2(SF2424+G20) 직결 → J2 허브(⌀56 + 6805 보스, 베인 r47) → 상완 -Y 측판. **J3** = +Y 타워 외면에 A3(SF2423+G20)를 J2와 동축 직결 → 크랭크 → 구동 링크(길이 = L1 현, 625ZZ 양단, ⌀5 핀) → 혼 핀 → 하완 — **평행사변형: 크랭크 월드각 = 하완 월드 피치**. 팔꿈치 = 수동 피벗(숄더 ⌀8×30 ×2 + 상완 측판 608ZZ ×2, ±Y 스페이서 ⌀12×8). **링크 = forearm 중앙면(y=0)** (v0.18, 사용자 요구 — 측판 사이 중앙 채널 주행): 크랭크는 J2축 중앙 디스크(⌀52, y4.7~11.7) + ⌀25 긴 보스가 +Y 측판 6805ZZ(인-플레이트 y30~37) 통과 → 외단 ⌀52 플랜지(y37.5~44.5)에 커플러 결합(캔틸레버 ~22mm). 혼 = forearm 양 측판 일체 ⌀5 숄더 핀(R36.5@horn_phase 155°, 별도 부품·아크 슬롯 폐지) — 핀이 평행사변형 힘을 양 측판에 대칭 전달, 링크 625ZZ는 중앙·양측 스페이서 ⌀8×12.5. 상완 내폭 75→60, 타워 스팬 124.5. L1=225/L2=275 유지
- **꺾임 빔 (팔레타이저 기구학 오프셋, v0.17)**: `ua_bend_deg=15 / fa_bend_deg=30`, 무릎은 현(chord) 위쪽 볼록(hull 충전 단면 — 현 아래 모서리는 직선 = 스탠드오프 벨트 z-16). 기구학 기준은 항상 **현**(관절축 잇는 선) — 접힘각(j3-j2) 0° = 현 일직선 = 특이점이며 `env_fold_max = -15°`가 가드, `env_fold_min = -132°`는 check=2 실측(하완판↔상완 스탠드오프). 꺾임 덕에 외관상 곧게 펴져도 현은 일직선 미도달, 하완 꺾임이 저자세에서 팔꿈치 후방 돌출 억제. J2 축은 J1 축 전방 `j2_fwd=30` 오프셋
- **동일 플레이트 원칙 (v0.17 강화)**: 측판·베이스판 전 홀 관통(카운터보어 없음) → **평행이동 장착 (플립 불요)** — 꺾임 빔과 양립. 상완 ×2 / 하완 ×2 / 베이스 ×2 각 1형상. 타워만 예외(M3 카운터보어 한면) — x대칭 프로파일 + 플립 쌍. 한쪽 전용 기능은 볼트온 소부품(J1/J2 허브, 팔꿈치 혼). 경량 관통창 금지
- 출력 제약: 서포트 없는 출력 우선, 불가피하면 부품 분할+볼트 체결 (MFG-003). 단일 부품 260×260×255mm 이내 (MFG-004, SparkX i7 — 측판 277은 베드 대각 배치). 3축 밀링 가공 가능 형상 유지 (GEN-002)

### 전장 인터페이스 (장착부만 제공)

전장(드라이버, 전원, 컨트롤러, 배선)은 범위 외. 단, 기구 모델은 J1~J3 각각에 NEMA 17 모터 마운트(⌀22 파일럿 + M3×4)·리미트 스위치(limit switch) 장착부(J1: 베이스판/터릿 베인, J2: 타워/허브 베인, J3: 상완 측판/혼 베인)·케이블 가이드(SHOULD)를 제공한다.

## 명령어

OpenSCAD STL 내보내기 (다피스 부품은 `piece` 파일 변수로 선택):
```
openscad -o export/stl/<부품명>.stl parts/<부품명>.scad
openscad -o export/stl/upper_arm_plate.stl -D "piece=\"plate\"" parts/upper_arm.scad
```

`main.scad` = Customizer 뷰어 진입점: `part`(assembly/exploded/print/단일 부품), `[Pose]` J1~J3, `[Labels]` show_labels/label_size/label_detail(세부 라벨은 얇은 지시선+순번만 표시, 와셔/유틸 피처까지의 순번-이름-수치 매핑은 `assembly/assembly.scad` 주석 참조), `[Hardware]` show_hardware(베어링·볼트·너트·스페이서 목업), `[Exploded]` explode. 컬러는 **assembly/main 호출부에서 부여** (`parts` 모듈은 `col` 파라미터만 적용하고 `C_카테고리` 상수를 직접 참조하지 않음). `lib/colors.scad`는 의미 카테고리 기본색 + 조립 변형색(rgb 벡터)을 제공해 같은 부품도 배치별 톤 차이를 줄 수 있다. 구매품(vitamin)은 **optional `col` 인자**(rgb 벡터)를 받아 내부에서 톤온톤 파생(`col*0.45` 어둡게/`col*1.3` 밝게) — 예: `bearing`은 외/내륜 = col, 실드 밴드 = col*0.45. 리미트 스위치(`limit_switch`)는 KW12형 롤러 레버 + 단자 포함, 몸체/레버/롤러/핀 색 구분 — 3개소(J1 베이스판/J2 -Y 타워/J3 상완 +Y) 배치·라벨(LS1~3, 적색). `assembly/assembly.scad`의 `robot(j1,j2,j3,e,labels,lsize,hardware,label_detail)` 모듈이 본체 — CLI 간섭 검사(check=2~5)는 `-D J1a/J2a/J3a=...` 파일 변수로 동작하며 hw 기본 false라 체결류 면접촉 노이즈 없음 (check=3/5는 스탠드오프 포함 위해 해당 측만 hw=true).

## 문서화 규칙

- 공학 용어는 '한국어(영어)' 병기 (DOC-002). 예: 유성 기어(planetary gear)
- 유지 의무 문서: `docs/bom.md` (부품명·규격·수량·단가·구매처 링크)

## 검증 도구

- `tools/check_asm.ps1` — 간섭 검사 그리드 (check=2: 하완∩상완 / check=3: J1 체인∩고정부 / check=4: 링키지∩구조 / check=5: 암∩터릿·타워). 빈 STL = 통과 — v0.17 형상 기준 그리드 재구성 필요 (J1 차원 추가). 스탠드오프(육각 몸체)는 hw 게이트 밖 상시 표시 — 간섭 검사에 포함됨
- `tools/dbg_section.scad` — 수평 단면 디버그 (z = J2 축 + sec_z) — y-스택 체결 검증용
- `tools/mass_report.ps1` — STL 체적 → 질량(g)·바운딩 박스 리포트 (MEC-007/MFG-004 검증)
- 형상 변경 시 두 스크립트 재실행

## 현재 상태

팔레타이저식 하부 구동 재확장 (2026-06-13, v0.17) — 베이스→터릿(J1)→상완(J2)→하완(J3 원격) 3축.
좌표 원점 = J1 축 ∩ 베이스판 상면. J2 축 = (j2_fwd 30, 0, 95). 모터 3기 전부 베이스측 (이동부 모터 0).
링크·혼 = 상완 포크 내부 수납 (사용자 요구 — 외부 노출 없음), 꺾임 15°/30° (공격적 도그레그).
출력 부품 10종: base_plate ×2 (동일), j1_ring, turret_plate (6810 내륜 보스 일체), tower_plate ×2 (동일·플립 쌍),
foot_bracket ×4, drive_hub_j2 (J1은 터릿 보스 일체), upper_arm_plate ×2 (~277, 꺾임 15°, 슬롯 폐지),
forearm_plate ×2 (~277, 꺾임 30°, 혼 핀 러그 R36.5+베인 일체), j3_crank (중앙 디스크 ⌀52 + 긴 보스 + 커플러 플랜지),
drive_link (L225 중앙), cable_clip ×6. (elbow_horn 폐지 → forearm 측판 일체 핀)
사용 베어링 = CON 4종 전부: 6810 ×1 (J1), 6805 ×2 (J2), 608 ×2 (팔꿈치), 625 ×2 (링크).
숄더 볼트: ⌀8×30 ×2 (팔꿈치), ⌀5×20/×16 (크랭크/혼 핀 — 와셔 조정).
엔벨로프 (간섭 실측): 접힘각 j3-j2 ∈ [-132, -15], j2 ∈ [0,75] 잠정, j1 ±90 잠정.
축 직결 = 기성 플랜지 커플러(set-screw, ⌀8 보어·플랜지 ⌀50·전고 17.5·M5) (v0.18) —
분할 클램프(슬릿+M3 핀치) 폐지. J1 6810 회전 내륜 전환(터릿 보스), drive_hub j1 폐지, 베이스판 J1 중심 ⌀25 개방.
**v0.18 링크 중앙화**: 구동 링크를 상완 포크 +Y(y33)에서 forearm 중앙면(y=0)으로 이동 — 크랭크 중앙 디스크+긴 보스,
혼=forearm 측판 일체 핀(elbow_horn 폐지), 상완 아크 슬롯 폐지, 상완 내폭 75→60, 타워 스팬 124.5.
검증 완료 (2026-06-13): 전 파일 .csg 컴파일, 렌더 검사 (전체/팔꿈치/J2/크랭크/베이스/극단 포즈),
check=2~5 엔벨로프 내 전 케이스 통과 (j2_vane_r 65→47 축소로 타워 스탠드오프 충돌 해소,
커플러 타워 확장 후 재검 통과).
잔여: 간섭 그리드 자동화 (check_asm.ps1 재구성), 질량 재계측, 베인 위상 3종 검증,
크랭크/혼 핀 잼 너트·와셔 조정 확정, SF2422/2424 몸체 치수 데이터시트 확인, BOM 단가·링크.
참고: REQUIREMENTS.md·docs(design-notes 등)는 삭제됨 — 요구사항 ID 참조는 유산 표기.
