// 디버그(debug): 리미트 스위치(limit switch) 단품의 레버(lever), 롤러(roller), 색상(color)을 확인합니다.
//
// 용어
// 1. 디버그(Debug)
// 2. 리미트 스위치(Limit switch)
// 3. 레버(Lever)
// 4. 롤러(Roller)
// 5. 단품(Individual part)
use <../lib/vitamins/kw12_limit_switch.scad>

kw12_limit_switch();
